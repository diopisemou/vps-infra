# One line per thing to include in the backup, per server. Wildcards ok.
# Format: <type>:<path-or-container-name>
volume:*
compose:/opt/*/docker-compose.yml
etc:/etc/vps-infra
etc:/etc/ssh/sshd_config
